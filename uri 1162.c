#include<string.h>
int main()
{
    int a,b,c,i,j,l,k;
    scanf("%d",&a);
    for(i=0;i<a;i++){
        scanf("%d",&b);
        int ar[b+1];
        for(j=0;j<b;j++){
            scanf("%d",&ar[j]);
        }
        c=0;
        for(j=1;j<b;j++){
            if(ar[j]>ar[])
        }

        printf("Optimal train swapping takes %d swaps.",c);
    }
    return 0;
}
